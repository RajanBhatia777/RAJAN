if(true){
    var i="rajan";
    console.log(i); // if we are using var then i will be print outside the scope or inside also

}   
    console.log(i)

    console.log(i); // if we are using the let datatype then it will be use only inside the function not out side

//A block is a chunk of code bounded by {}. A block lives in curly braces. Anything within curly braces is a block.